<div class="user_info">
  <a href="<%= user_path(current_user) %>">
    <span class="user_name">
      <%= current_user.name %>
    </span>
    <span class="shelves">
      <%= pluralize(current_user.shelves.count, "shelves") %>
    </span>
  </a>
</div>

